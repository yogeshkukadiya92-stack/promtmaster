const call=async(path,token,options={})=>{if(!token)return null;const response=await fetch(path,{...options,headers:{authorization:`Bearer ${token}`,accept:"application/json",...(options.body?{"content-type":"application/json"}:{})}});if(!response.ok)return null;return response.json();};
export const loadEnterpriseUsage=async token=>(await call("/api/enterprise-usage",token))?.usage||null;
export const updateEnterpriseFlag=async(token,key,enabled)=>(await call(`/api/enterprise-usage/flags/${encodeURIComponent(key)}`,token,{method:"PATCH",body:JSON.stringify({enabled})}))?.usage||null;
export async function downloadEnterpriseUsage(token){const response=await fetch("/api/enterprise-usage/export",{headers:{authorization:`Bearer ${token}`}});if(!response.ok)return false;const blob=await response.blob();const url=URL.createObjectURL(blob);const anchor=document.createElement("a");anchor.href=url;anchor.download=`intentos-usage-${new Date().toISOString().slice(0,10)}.json`;anchor.click();URL.revokeObjectURL(url);return true;}
